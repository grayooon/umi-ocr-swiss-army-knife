# 参与项目

问题反馈请说明系统/位数、Umi 版本、插件版本、模型模式、从零安装还是沿用依赖、复现步骤及完整报错。优先使用可公开的最小样例，不要提交业务账号、敏感图片或真实网站会话数据。

修改代码时先创建自己的分支，保持一个 PR 聚焦一个问题。说明为什么修改、改变了什么、实际如何验证。运行 `python -m unittest discover -s tests -v` 检查源码/通信/安装器回归；它不替代 Windows 推理与离线验收。

不要在插件入口或父进程桥接中导入 numpy、cv2、PIL、ddddocr 或 onnxruntime。依赖只在 worker 中导入，避免不同插件共享不兼容 DLL。不要用清空 sys.modules 或全局 taskkill 切换运行时。

改变下载来源、依赖或模型时，更新来源、固定版本和 SHA-256，核对目标 Python、ABI、Windows 与模型 IR，并保留第三方许可。不能为了让下载继续而关闭证书校验或哈希校验。

当前未配置自动发布工作流：正式版需维护者完成实际 Windows 验收，再手工创建与源码对应的 Release。Win7 的结果以实机及确切依赖文件为准。
